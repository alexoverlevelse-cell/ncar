# NCar — Telegram Mini App

Маркетплейс: доверенный список людей публикует свои автомобили на продажу и свои услуги. Остальные пользователи только просматривают и связываются с продавцом/исполнителем услуги напрямую в Telegram.

Это не витрина одной компании — карточку машины или услуги создаёт и редактирует сам человек, которому выдан доступ, а не единый администратор.

## Стек

- **Frontend**: Next.js (React) + Telegram WebApp SDK
- **Backend/БД/Auth/файлы**: Supabase (Postgres + Storage + Auth)
- **Хостинг**: Vercel (frontend), Supabase (бэкенд)

## Модель доступа

- Публиковать (создавать/редактировать/удалять свои объявления) может **только пользователь из списка доверенных публикующих** — таблица `allowed_publishers` (Telegram ID → активен/неактивен).
- Обычный пользователь (не в списке): видит все опубликованные машины и услуги, может написать автору в Telegram, оставить заявку. Не может ничего публиковать.
- Публикующий редактирует и удаляет **только свои** объявления, не чужие.
- Права проверяются **на сервере** по Telegram ID из подписанного `initData`, а не только скрытием кнопки в интерфейсе.
- Добавление/удаление человека из списка доверенных — ручная операция суперадминистратора (владельца проекта), не самостоятельная регистрация.

## Масштаб MVP

Ожидается **не больше 10 машин одновременно**, все в одном списке, без категорий и без разделения по маркам. Из-за этого в MVP **не нужны**: фильтры по марке/категории, разделы-категории в навигации, пагинация. Простой единый список карточек — уже достаточно, искать в нём нечего. Если решите добавить фильтры/категории позже — сначала обновить этот раздел.

## Данные (черновая модель, уточнять по ходу разработки)

**cars**: owner_telegram_id, марка, модель, цена, год, пробег, топливо, коробка, кузов, объём/мощность двигателя, цвет, описание, комплектация, статус, фото (галерея), created_at, updated_at

Поля марка/модель остаются как есть (это данные конкретной машины), но самостоятельного раздела "категории" или страницы фильтра по марке в MVP нет — см. "Масштаб MVP" выше.

**services**: owner_telegram_id, название, описание, цена или "по запросу", фото, длительность, статус (active/inactive)

**allowed_publishers**: telegram_id, имя, активен (bool), added_at

Статусы машины: `draft`, `available`, `reserved`, `sold`, `hidden`.

## Правила

- Не создавать форму самостоятельной регистрации в список публикующих — только ручное добавление владельцем проекта.
- Не показывать VIN и другие приватные поля публично.
- Не реализовывать оплату внутри Telegram в MVP — только связь с продавцом.
- Все опасные действия (удаление машины/услуги) — только с подтверждением.
- Не выдумывать характеристики машин, цены, тексты компании — если данных нет, спросить.
- Секретные ключи (Supabase service role и т.п.) — только в серверных переменных окружения, никогда в клиентском коде.

## Структура проекта

```
src/app/                    # App Router: / (главная), /cars, /cars/[id], /services, /contact
src/components/             # BottomNav, CarCard, Logo, PageHeader, DemoNotice, icons
src/types/                  # Car, Service + списки колонок для select в Supabase
src/lib/site-config.ts      # название, регион, валюта, контакты + buildTelegramLink()
src/lib/data.ts             # загрузка машин/услуг: Supabase, иначе фоллбэк на демо
src/lib/demo-data.ts        # демо-машины и услуги для показа, пока нет базы
src/lib/format.ts           # формат цены, пробега, чисел
src/lib/supabase.ts         # клиент для браузера (anon key, только чтение по RLS)
src/lib/supabase-admin.ts   # серверный клиент (service role) — только в API-роутах
src/lib/telegram.ts         # клиентский доступ к window.Telegram.WebApp, initData
src/lib/telegram-auth.ts    # серверная проверка подписи initData + isAllowedPublisher()
supabase/schema.sql         # таблицы cars, services, allowed_publishers + RLS-политики
.env.example                # список нужных переменных окружения (без значений)
AGENTS.md                   # генерируется Next.js автоматически, не редактировать
```

**Дизайн**: тёмная тема, оранжевый акцент, нижняя навигация из 4 пунктов —
по макетам в `Дизай промті/` и `9d81da3d-*.png`. Токены цветов в
`src/app/globals.css`. Светлой темы намеренно нет.

**Демо-данные**: пока Supabase не подключён, страницы показывают содержимое
`demo-data.ts` с пометкой `DemoNotice`. Когда появятся реальные объявления,
удалить фоллбэк в `data.ts` вместе с `demo-data.ts` и `DemoNotice`.

**Как работает запись данных**: клиент никогда не пишет в Supabase напрямую.
Публикация/редактирование идёт через API-роут в `src/app/api/*`, который:
1. получает `initData` от клиента (`getInitData()` из `telegram.ts`);
2. проверяет подпись через `verifyTelegramInitData()`;
3. проверяет `isAllowedPublisher(user.id)`;
4. только после этого пишет через `supabaseAdmin`.

Такие API-роуты ещё не созданы — это следующий шаг разработки.

**Команды**:
- `npm run dev` — локальный запуск
- `npm run build` — проверка сборки

## Связанные файлы

- [PROMPT_CONTEXT_AUTO_TELEGRAM_APP.md](PROMPT_CONTEXT_AUTO_TELEGRAM_APP.md) — черновой промпт для ChatGPT с бизнес-контекстом. Частично устарел: (1) описывает модель "только админ публикует" вместо доверенного списка публикующих; (2) описывает фильтры/сортировку/категории по маркам — не нужны при ≤10 машин, см. "Масштаб MVP" выше. При расхождениях с CLAUDE.md — верить CLAUDE.md.
- @AGENTS.md — сгенерировано Next.js автоматически, содержит заметки о версии фреймворка. Пересоздаётся при `next dev`, не редактировать вручную.
